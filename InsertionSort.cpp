#include<bits/stdc++.h>

using namespace std;

void insertionSort(int arr[], int n)
{
	for (int i = 0; i < n; i++)
	{
		int tmp = i;
	}
}

int main()
{
	int n,arr[100000];
	cin >> n;
	for (int i = 0; i < n; i++)
		cin >> arr[i];
	insertionSort(arr,n);
}