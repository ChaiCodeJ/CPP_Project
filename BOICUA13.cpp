#include<bits/stdc++.h>

using namespace std;


int main()
{
	long long multip = 13,l,r,sum = 0;
	cin >> l >> r;
	sum = (r+l)*((r-l)+1)/2;

	while (multip <= r)
	{
		(int i = 1; i <= n)
	}
	cout << sum;
}

